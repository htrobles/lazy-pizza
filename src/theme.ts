import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E15C31',
    },
    text: {
      primary: '#fff',
    },
  },
});

export default theme;
